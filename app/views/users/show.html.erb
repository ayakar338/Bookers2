
<div class="container px-5 px-sm-0">
  <div class="row">
     <div class="col-md-3">
      <%=render "users/list",user: @user%>
      <%=render "books/list",book: @book%>
     </div>
     <div class="col-md-8 offset-md-1">
      <table class="table table-striped">

       <h1>Books</h1>
　　　<tbody>
　　　<thead>
       <tr>
        <th class="img"></th>
        <th>Title</th>
        <th>Opinion</th>
       </tr>
      </thead>


      <% @books.each do |book|%>
        <tr>
          <%=link_to user_path(book.user.id) do%>
         <td><%= attachment_image_tag book.user, :profile_image, :fill, 50, 50, fallback: "no_image.jpg" %>
          <%end%>
         </td>
         <td>
          <%=link_to book_path(book.id) do%>
             <%=book.title%>
          <%end%></td>
         <td><%=book.body%></td>
         <%end%>
        </tr>
     </tbody>
     </table>
     </div>
  </div>
</div>

<%=render "homes/list"%>
